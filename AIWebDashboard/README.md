# 🖥 AI Web Dashboard (LEGACY)

Standalone Python-stdlib dashboard from the early droplet era: polls MySQL
`gameserver.characters` + driver logs and serves a table on :8199 (`start_dashboard.sh`).

**Superseded** by the engine's built-in dashboard (`AIPlayerEngine` `web/` package, served by
FleetPlay on :8210 with `/api/v1/*`, map, ops page and token auth) — see the repo-root
`README.md`. Kept only as a DB-direct query reference; not part of the live fleet ops.
