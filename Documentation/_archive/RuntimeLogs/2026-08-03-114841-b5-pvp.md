# 2026-08-03 — B5: live PvP proof — PROVEN

**Agent:** System (volodro) · **Board:** Stream B (B5) · Spec: `Audit/36-b5-live-pvp.md`

## Original prompt
"Start magnifico of B5 (code it :))" — B5 = live PvP proof (two AI players fight each other).

## Objective
Prove two AI bots PvP over the external socket: verify mutual server `Attack`(0x05) packets attributed to
each player (objectId 2 = CombatBot_01, 3 = CombatBot_02) + a DB damage/Kill signal.

## Files modified (added)
- `AIPlayerEngine/.../examples/PvPProbe.java` (two GS connections; mutual Action/AttackRequest; reader
  threads tally Attack(0x05) by attacker objId)
- `scripts/b5_pvp_prove.sh` (position/heal both bots → restart LS → run PvPProbe → assert)
- `Documentation/Audit/36-b5-live-pvp.md` (spec + verified result)
- `Documentation/RuntimeLogs/2026-08-03-114841-b5-pvp.md` (this file)
- DB: positioned/healed CombatBot_02 (was charId3 level1, void/dead); CombatBot_01 kept at Wolf zone.
- Knowledge base: START_HERE.md, STATUS.md, SESSION_HANDOFF.md, TASKS.md (task 61), ai_progress_report (B5 done).

## Problems & solutions
- **Multi-statement `mysql -e` only applied the first UPDATE** → run each char's UPDATE separately.
- CombatBot_02 had NULL curHp/maxHp (created default) → set explicit hp/mp (126/38) so it loads alive.
- **stdout block-buffering to file hid the attacker-objId tally lines** in a first grep (pattern didn't
  include "attacker objId"); full log confirmed them. No code bug.
- Verified PvP path: `AttackRequest`(0x0A) on a player → `Creature.onForcedAttack` → PvP flag + attack
  (blocked only in peace zones / non-attackable / confound); no newbie-protection gate in this config.

## Verification (pasted) — B5 PROVEN
```
=== A's connection Attack attacker-objId -> hits ===
  attacker objId 2 : 13 hits   # CombatBot_01
  attacker objId 3 : 12 hits   # CombatBot_02
=== B's connection Attack attacker-objId -> hits ===
  attacker objId 2 : 13 hits
  attacker objId 3 : 12 hits
PVP PROVEN (mutual player-vs-player attacks) = true
```
DB: `CombatBot_02` curHp **126 → 120** (took real PvP damage). No L2JM server source changed. Both
online=0 after clean probe exit. `mvn clean compile` → BUILD SUCCESS.

## Remaining issues / next steps
- B6–B10 (quest, trade proof) next. Alternatively wire the proven PvP/PvE packets into `CombatAI`/`PacketLogger`
  (Stream C) — `PacketLogger.parseNpcInfo` off-by-one still to fix.
- The other 23 `ai_%` chars remain in the void (16600,17000,434) → relocate before further proofs.
- One live attempt's verdict used two separate bots; a future B could prove a PvP **kill** (pkkills/karma).
