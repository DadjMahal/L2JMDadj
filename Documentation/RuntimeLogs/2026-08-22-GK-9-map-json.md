# RuntimeLog — 2026-08-22 GK-9 — map.json (travel-nav datapack)

## Task
GK-9 — Map extractor → `map.json`: teleports, routes, zones, spawn regions (travel nav).

## What was done
`scripts/datapack/extract_map.py` — one extractor, four kinds (every record carries `id`+`kind`):

| kind | count | source | content |
|---|---|---|---|
| teleporter | 245 | `teleporters/**/*.xml` (145 files) | per (npc,type) record: category, type (NORMAL/OTHER/NOBLES_*), 1,407 destinations `{name,x,y,z,feeId,feeCount}` |
| zone | 2533 | `zones/*.xml` | Town/Peace/NoLanding/pvp footprints: name, type, shape, minZ/maxZ, raw node polygon (x/y) + centroid |
| route | 14 | `Routes.xml` | scripted NPC walk routes, 229 waypoints `{x,y,z,delay,run}` |
| spawnRegion | 203 | npcs.json (10,754 spawn rows) | per zoneHint prefix: centroid + spawnCount + minLevel..maxLevel level band ("where do I hunt at N") |

- **Honest flag (GK-5 pattern):** boss/raid/event zones (Valakas lair, Seed of Annihilation,
  Tiat Movie…) sit outside the playable-world box — real datap locations but NOT travel-nav
  areas. They stay in map.json with `needsReview:true` (never dropped, never zeroed, never faked).
- `validate.py`: `map.json` added to EXPECTED; coordinate checks generalized to
  spawns/destinations/nodes/points. Zone nodes are x/y-only (legitimately z-less footprints);
  flagged zones exempt from the world-box invariant (documented outlier).
- `extract_all.py` now runs 9/9 domains. `SCHEMAS.md` §map.json contract.
- `MapKnowledgeTest` (7 tests) locks the REAL data with the engine's own dependency-free
  JsonResource: four kinds present, Roxxy(30006)→Dwarven Village (115120,-178112, real fee),
  Giran Castle Town 42-node polygon, teleport dests + route waypoints strictly in-world,
  level bands sane.

## Pasted (audit acceptance)
```
[map] teleports=245 zones=2533 routes=14 spawnRegions=203 -> map.json   (2,995 records; map.json 1.6MB)
[validate] files=9 issues=0
[extract_all] done: 9/9 domains ok
GATE GREEN — 560/560 tests (was 553), style 0 violations, secret-lint clean
```

## Evidence / gate
- One feat commit `e1bdb9de` (code+data+tests) + this docs commit; pushed to master.
- Note: the stale GK-8 WIP (`KnowledgeBase.parseShops`, uncommitted, no tests) was stashed
  BEFORE this commit so the GK-9 diff stays surgical; restored after (`git stash pop`).