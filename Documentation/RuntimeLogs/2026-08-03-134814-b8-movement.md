# Runtime Log — 2026-08-03 13:48 — B8: live MOVEMENT PROVEN

## What was proven
**B8 = PROVEN** — the AI player deliberately walked its real character to a destination over the external
socket, and the server confirmed + persisted the movement.

Probe: `AIPlayerEngine/.../examples/MoveProbe.java` (single account `ai_combat_01` / CombatBot_01).

## Evidence
1. **Wire** (MoveProbe stdout, after sending `MoveToLocation`(0x01) to (-82515,241221,-3728)):
   - `CHAR_MOVE_TO_LOCATION(0x01) = 17`
   - `VALIDATE_LOCATION(0x61) = 1`
   - `STOP_MOVE(0x47) = 0`, `NPC_INFO(0x16) = 37`, `SYSTEM_MESSAGE(0x64) = 2`
2. **DB** (`gameserver.characters`, CombatBot_01, both reads online=0):
   - Before: `-83789 240799 -3717` (B7 had left it at Silvia's spot)
   - After:  `-82515 241221 -3728` = **exact arrival at the requested destination**

## Protocol notes (for Stream C wiring)
- Client `MoveToLocation`(0x01) layout: `[targetX][targetY][targetZ][originX][originY][originZ][moveType:int]`
  (0=cursor-key walk, 1=mouse). Server auto-paths and broadcasts `CHAR_MOVE_TO_LOCATION`(0x01) continuously
  while walking; `VALIDATE_LOCATION`(0x61) on registration.
- Reused the proven B3/B4 login+enter-world flow (LS auth → GS key exchange (packetEncryption=0 plaintext) →
  CharSelect → `EnterWorld`(0x03) → settle). No L2JM server source changed.
- The 3s settle after EnterWorld is enough to be in-world and accept a MoveToLocation; a plain Action click
  is not required for movement.

## State after
- CombatBot_01 now rests at (-82515,241221,-3728) (Trader 30040 spot), online=0. Adena 500k intact.
- B7 (trade) remains IN PROGRESS / blocked on buy-dialog validation (`Audit/38`).
- Working tree will be committed with Audit/39 + this log + KB sync.
