# Runtime Log — 2026-08-03 14:50 — B7: live TRADE PROVEN (genuine merchant buy)

## What was proven
**B7 = PROVEN** — an AI player bought an item from a real merchant NPC; the server **deducted adena and added
the item** to the bot's inventory (verified in `gameserver.items`). No L2JM server source changed.
This was the last remaining live gameplay proof.

## From "blocked" to PROVEN — real root cause (correcting the earlier misdiagnosis)
The earlier "blocker" (Silvia has no HTML menu / bypass cannot be validated) was WRONG. Silvia IS a `Merchant`
(`data/html/merchant/30003.htm` exists). Two real requirements were missing:
1. **Two `Action`(0x04) clicks**: `NpcClick.onAction` targets on click #1 and opens the dialog only on click #2.
   The probe now sends 2 Actions (700ms apart, respecting the action flood protector).
2. **Full server-cached bypass** `npc_<objId>_Buy <listId>` (not `Buy <listId>`): the HTML action is
   `bypass -h npc_268439739_Buy 3000300`; `HtmlUtil.buildHtmlBypassCache` strips `-h ` and caches
   `npc_268439739_Buy 3000300`; `RequestBypassToServer.validateHtmlAction` exact-matches, so the probe sends that
   exact string extracted verbatim from the received `NpcHtmlMessage`(0x0F).
3. Bot within `Npc.INTERACTION_DISTANCE`(250) of Silvia (positioned at her spawn).

## End-to-end live flow (TradeProbe)
1. Found Silvia (objId 268439739) from NPC_INFO.
2. 2× Action → NpcHtmlMessage(0x0F), the shop HTML with Buy links.
3. Extracted bypass `npc_268439739_Buy 3000300` → RequestBypassToServer(0x21).
4. Server → BuyList(0x11): money=500000, listId=3000300, 13 items.
5. Picked itemId 118 at price 75 → RequestBuyItem(0x1F) [listId][1][118][1].

## DB proof (`gameserver.items`, owner_id 2), two consecutive runs
- Adena (57): 500000 → 499925 → 499850 (each run −75 = item 118 price).
- Item 118: two new rows added (count 1 each, INVENTORY).

**B7 ══ PROVEN.** Opcodes used: NPC_INFO 0x16, Action 0x04(c), NpcHtmlMessage 0x0F(s), RequestBypassToServer
0x21(c), BuyList 0x11(s), RequestBuyItem 0x1F(c). Adena item_id=57.

## State after
CombatBot_01 at Silvia (-83789,240799,-3717), adena 499850 + item118 ×2. All B-stream live proofs now done:
B4 PvE, B5 PvP, B6 quest, B7 trade, B8 movement, B9 chat, B10 party — **all PROVEN**.
Working tree committed with Audit/38 (corrected + PROVEN), TradeProbe, scripts/b7_trade_prove.sh, this log, KB sync.
