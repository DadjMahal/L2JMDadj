# RuntimeLog — 2026-08-22 GK-11 — trainers.json (skill trainer mapping)

## Task
GK-11 — SkillLearn/class-tree loader: trainer NPC mapping per profession (class change support).

## What was done
`scripts/datapack/extract_trainers.py` — one record per skill-trainer NPC:

| field | value |
|---|---|
| trainers | **217** (from SkillLearn.xml) |
| classIds | **1,364** rows / **89** distinct classes — every one verified inside classes.json chains (0 missing) |
| name | resolved from npcs.json |
| spawn | real world position — **217/217** covered |

- **Honest data gap found & fixed:** npcs.json (GK-2) only kept POLYGON spawns (mob regions,
  ids 18001-27215); village NPC point-spawns (`<npc id x y z/>` in `spawns/**`) were skipped.
  GK-11 parses those point rows separately — every trainer now carries a real in-world
  location (e.g. Auron 30010 at Gludin -81861,149197,-3125).
- `validate.py` 11/11, `extract_all.py` 11/11 (extract_trainers added last), SCHEMAS.md
  §trainers.json (honest note on point-vs-polygon spawns).
- `TrainerKnowledgeTest` (6) locks real data: 100+ trainers all with in-world spawns +
  classIds, Auron 30010 = Human Fighter chain 0..9 at Gludin, Zigaunt 30022 = Human Mystic
  line (10/15/16/17), classIds ⊆ classes.json chains, names resolved, spread across towns.

## Pasted (audit acceptance)
```
[trainers] trainers=217 (with-spawn 217) classes=89 -> trainers.json
[validate] files=11 issues=0
[extract_all] done: 11/11 domains ok
mvn: Tests run: 573, Failures: 0, Errors: 0  (567→573)
```

## Evidence / gate
- Claim `abd92cf2` + feat `ba83a0f5` (code+data+tests) + docs commit; pushed to master.
- gate.sh: GATE GREEN (tests 573 · style 0 · secrets 0).
- The stale GK-8 WIP (`KnowledgeBase.java`, uncommitted) stays untouched.