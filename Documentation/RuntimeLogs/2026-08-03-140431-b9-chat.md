# Runtime Log — 2026-08-03 14:04 — B9: live CHAT PROVEN

## What was proven
**B9 = PROVEN** — an AI player sent a chat message that the real L2JM server processed and delivered to
another player. `ChatProbe` (single JVM, two bot connections).

## Evidence (ChatProbe stdout)
- Both accounts entered the world: A = ai_combat_01 (CombatBot_01), B = ai_combat_02 (CombatBot_02).
- A sent client `Say2`(0x38) **whisper** to CombatBot_02 with run-unique token `B9WHISPER_621452`.
- **B's connection:** CREATURE_SAY(0x4A)=3, **1 containing the token** (len 69) = the server-delivered whisper.
- **A's connection:** CREATURE_SAY(0x4A)=3, **1 containing the token** (len 73) = the `->CombatBot_02` echo.
- `B received the whisper token = true`, `A saw its own echo = true` → **CHAT PROVEN = true**.

## Protocol notes (for Stream C wiring)
- Client chat `Say2`(0x38) DHCP: `[0x38][text:UTF-16LE null-term][type:int][target:UTF-16LE null-term (whisper)]`.
  ChatType WHISPER client id = 2.
- Server chat broadcast `CREATURE_SAY`(0x4A): `[0x4A][senderObjId:int][chatType:int][senderName][text]`.
- Whisper path (`ChatWhisper` handler): no level gate, no range limit — delivers via `World.getPlayer(target)`.
  GENERAL chat is gated to `MinimumChatLevel`=20 (both bots are L1-2) — that is why whisper was used.
- Reused the proven B3/B5 login+enter-world flow (two accounts, classic sockets, plaintext GS channel,
  packetEncryption=0). No L2JM server source changed.

## State after
- Both bots logged out cleanly (probe closed connections). Working tree will be committed with Audit/40 +
  this log + KB sync + scripts/b9_chat_prove.sh.
- B7 (trade) still IN PROGRESS / blocked (Audit/38). B4/B5/B6/B8/B9 live PROVEN.
